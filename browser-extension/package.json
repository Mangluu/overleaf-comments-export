{
  "name": "overleaf-comments-export-extension",
  "version": "1.1.0",
  "private": true,
  "description": "Manifest V3 browser extension for exporting comments from the current Overleaf project.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check popup.js && node --check src/export-core.js && node --check src/page-client.js"
  },
  "license": "MIT"
}
